# Model risk archive

Archived observation for reviewer testing.

The example source bundle treats the referenced model/API notice as a change-management signal. It is intentionally simple so validators can compare the subject, primary evidence, archive, and independent context without relying on a local mock calculation.
